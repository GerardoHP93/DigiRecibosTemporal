import 'package:flutter/material.dart';

class AppColors {
  // Colores primarios de la aplicación
  static const Color primary = Color(0xFF95B8D1);
  static const Color primaryLight = Color(0xFFB3D4E6);
  static const Color accent = Colors.blue;
  static const Color accentLight = Color(0xFF8BB8D4);

  // Colores de fondos
  static const Color background = Colors.white;
  static const Color cardBackground = Colors.white;

  // Colores de textos
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Color(0xFF757575);

  // Colores de categorías predeterminadas
  static const Color categoryGreen = Colors.green;
  static const Color categoryBlue = Colors.blue;
  static const Color categoryRed = Colors.red;
  static const Color categoryDefault = Colors.black;

  // Colores de estados
  static const Color error = Colors.red;
// Añadir estas constantes si no existen ya
  static const Color success = Colors.green;
  static const Color warning = Colors.orange;

  // Colores de bordes y sombras
  static const Color border = Color(0xFFDDDDDD);
  static const Color shadow = Color(0x33000000);
}
